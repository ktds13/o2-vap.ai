
1. Firstly, check changed file
2. add those files to staged changed
3. write appropriate commit with those files based on user prompt 
    - default is short one-line summary
    - commit message length shortcuts:
        . s -> Short (one-line summary)
        . n -> Normal (brief description)
        . l -> Long (detailed explanation)